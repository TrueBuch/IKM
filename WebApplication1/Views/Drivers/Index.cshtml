@model IEnumerable<WebApplication1.Models.Driver>
@{
    ViewData["Title"] = "Водители";
}

<h1 class="mb-3">Водители</h1>

<a asp-action="Create" class="btn btn-success mb-3">Добавить</a>

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(m => m.First().Surname)</th>
            <th>@Html.DisplayNameFor(m => m.First().Name)</th>
            <th>@Html.DisplayNameFor(m => m.First().PhoneNumber)</th>
            <th>@Html.DisplayNameFor(m => m.First().DriverStatus)</th>
            <th style="width:140px"></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Surname</td>
            <td>@item.Name</td>
            <td>@item.PhoneNumber</td>
            <td>@item.DriverStatus.GetDisplayName()</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">✏️</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">🗑️</a>
            </td>
        </tr>
    }
    </tbody>
</table>
